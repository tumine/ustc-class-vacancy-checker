1. 可以选择检测到有空位后自动选课，如果该选项被启用，则在检测到有空位后，自动点击“选课”按钮，然后等待选课结果，如果选课成功，则弹出消息提示“选课成功”，如果选课失败，则给出弹出的窗口中的对应报错消息
2. 目前只能完成一次完整的点击查询过程，第二次再进入查询流程后应用就无法自动点击
    - 输入密码后可以正常执行登录流程，但是从查询课堂号界面点击开始检测后无法进行自动登录
3. 实现另一个独立的按输入关键字查找课堂号的部件
    - https://catalog.ustc.edu.cn/query/lesson
    - 呈现按该关键字查找得到的所有课程并列出课堂号、课程名、授课教师，由用户选择需要跟踪的课程
4. 支持按顺序依次查找若干课程的空余情况
5. 把视图修改为适应屏幕的长宽缩放，而非塞满屏幕
6. 加入刷新网页的按钮
7. 更新应用图标
8. 去除课堂号输入框的输入内容联想

---

1. 输入查询关键字之后需要等待一段时间再读取结果
2. 查询到结果要自动加入跟踪课程列表中
4. 切换搜索的关键字类型时，清空搜索框
