1. 可以选择检测到有空位后自动选课，如果该选项被启用，则在检测到有空位后，自动点击“选课”按钮，然后等待选课结果，如果选课成功，则弹出消息提示“选课成功”，如果选课失败，则给出弹出的窗口中的对应报错消息
2. 把视图修改为适应屏幕的长宽缩放，而非塞满屏幕
3. 加入刷新网页的按钮
4. 更新应用图标
5. 去除课堂号输入框的输入内容联想


---

1. 后台定时刷新检测功能
    - 需要维护一个跟踪课程列表
    - 查找课程后，将用户点击选中的课程自动加入跟踪课程列表
        - 考虑实现课程多选
    - 支持按顺序依次查找若干课程的空余情况
