1. 可以选择检测到有空位后自动选课，如果该选项被启用，则在检测到有空位后，自动点击“选课”按钮，然后等待选课结果，如果选课成功，则弹出消息提示“选课成功”，如果选课失败，则给出弹出的窗口中的对应报错消息
2. 把视图修改为适应屏幕的长宽缩放，而非塞满屏幕
3. 加入刷新网页的按钮
4. 更新应用图标
5. 去除课堂号输入框的输入内容联想
6. 对于已选中课程，不应点击选课按钮（页面只有退课按钮，是否可以直接区分两种按钮有待进一步确认）

---

1. 后台定时刷新检测功能
    - 需要维护一个跟踪课程列表
    - 查找课程后，将用户点击选中的课程自动加入跟踪课程列表
        - 考虑实现课程多选
    - 支持按顺序依次查找若干课程的空余情况
2. 去除输入“搜索关键字”时的输入联想
3. 删除后台跟踪课程时加入确认弹窗
4. 加入对所有后台跟踪课程即时追踪一次的功能
5. 将课程添加到后台跟踪队列后，无法按选定的自动检测间隔检测课堂余量，始终显示“最近检测：尚未检测”
