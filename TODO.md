1. 优化后台跟踪队列的课程检测逻辑：把对于每个课程都重新打开一次网页改为在网页搜索框直接反复搜索替换，如果网络出错再改为重新打开网页

---

不紧急
1. 把视图修改为适应屏幕的长宽缩放，而非塞满屏幕（非必要？）
2. 加入刷新网页的按钮
3. 检测到当前课程已经选中时，不再显示课堂余量信息（非必要？）
4. 在后台跟踪队列中，相同课程号若已有课程选中，则自动跳过其余课堂的检测（非必要，耗费大于收益？）
5. 在后台跟踪队列中，若有课程已选中，则自动删除该课程（非必要？）
